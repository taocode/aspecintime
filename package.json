{
  "name": "aspecintime",
  "version": "1.0.0",
  "description": "A Spec in Time — literary sci-fi book series by M. Narf",
  "scripts": {
    "fetch:blog": "node scripts/fetch-blog-feed.mjs",
    "dev": "npm run fetch:blog && hugo server -DF --disableFastRender",
    "build": "npm run fetch:blog && hugo --gc --minify",
    "deploy": "firebase deploy --only hosting",
    "clean": "del-cli public/**"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/taocode/aspecintime.git"
  },
  "keywords": [
    "hugo",
    "tailwindcss",
    "sveltia-cms"
  ],
  "author": "taocode",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@tailwindcss/typography": "^0.5.19",
    "del-cli": "^5.1.0",
    "tailwindcss": "^4.3.0"
  }
}
